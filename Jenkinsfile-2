pipeline{
	agent {
		docker {
			image 'ubuntu:latest'
		}	
	}
	options{
		newContainerPerStage()
	}
	stages{
		stage('Build'){
			steps {
				sh 'uname -a'
			}
		}
		stage('Deploy'){
			steps {
				sh 'uname -a'
			}
		}
	}
}
